{% extends 'base-backend.html.twig' %}

{% block title %}Carousel{% endblock %}

{% block body %}
    <a href="{{ path('app_admin_carousel_index') }}"class="btn btn-dark btn-sm">Retour à la liste</a>
    <div class="d-flex justify-content-between align-item-center">
        <h1 class="mt-3">Carousel</h1>
        {{ include('admin_carousel/_delete_form.html.twig') }}
    </div>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ carousel.id }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ carousel.imageName }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ carousel.title }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ carousel.text }}</td>
            </tr>
            <tr>
                <th>Tag</th>
                <td>{{ carousel.tag }}</td>
            </tr>
        </tbody>
    </table>

    

    <a href="{{ path('app_admin_carousel_edit', {'id': carousel.id}) }}" class="btn btn-success" ><i class="align-middle" data-feather="edit-3"></i></a>

{% endblock %}
